
grant all privileges on owip.* to root@'%' identified by '123';

ALTER TABLE `base_dispatch_cadre`
	ADD COLUMN `type`  TINYINT(3) UNSIGNED NOT NULL COMMENT '类别，1任命 2免职' AFTER `dispatch_id`,
	ADD COLUMN `post` VARCHAR(50) NOT NULL COMMENT '职务' AFTER `name`;



#######清空因私出国境数据步骤：

    TRUNCATE `abroad_passport_draw_file`;

    delete from abroad_passport_draw;

    delete from abroad_passport where apply_id >0;

    delete from abroad_passport_apply;

    TRUNCATE `abroad_approval_log`;

    delete from abroad_apply_self;

###############################
