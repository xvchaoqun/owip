<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="persistence.cg.common.ICgMapper">
    <select id="getNeedAdjustMember" resultType="java.lang.Integer" parameterType="map">
        select cm.id from cg_member as cm left join
        (unit_post_view as upv left join cadre on (cadre.id = upv.cadre_id)) on upv.id = cm.unit_post_id
        where (cm.user_id !=cadre.user_id or isnull(cm.user_id) != isnull(cadre.user_id))
        and cm.is_current = 1 and cm.type = 1
        <if test="teamIds != null and teamIds.length>0">
            and cm.team_id IN
            <foreach collection="teamIds" item="teamId" separator="," open="(" close=")">
                #{teamId}
            </foreach>
        </if>
        order by cm.team_id desc;
    </select>
</mapper>